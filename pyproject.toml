[project]
name = "secure-campus-chatbot"
version = "2.0.0"
description = "具备提示注入防御、隐私保护与角色感知检索的智能校园助手系统"
requires-python = ">=3.10"
readme = "README.md"

[tool.pytest.ini_options]
testpaths = ["backend/tests"]
pythonpath = ["backend"]
addopts = "-v --tb=short"
